<h2><%= @story.name %></h2>
<h3>An Adventure by: <%= @story.user.username %></h3>

<h4><%= @story.summary %></h4>

<p><%= @story.story_outline %><p>

<div>
Journal Entries:<br>
<% @story.journals.reverse.each do |journal| %>
    <%= journal.created_at.strftime("%B %e, %Y at %I:%M %p") %><br>
    <%= journal.content %><br><br>
<% end %>
<%= link_to "Add a Journal Entry for this Story.", new_story_journal_path(@story.id) %>
</div>

<p><%= link_to "See #{@story.name}'s characters", story_characters_path(@story.id) %></p>
<p><%= link_to "Join this Story", new_story_character_path(@story.id) %>
